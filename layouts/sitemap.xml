{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- /* Tag and taxonomy listing pages were half the sitemap (46 of 94
       entries) while carrying no content of their own. Crawlers find them
       through page links; the sitemap stays for real pages. */ -}}
  {{ range .Data.Pages }}
  {{- if not (in (slice "taxonomy" "term") .Kind) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}
  </url>
  {{- end }}
  {{ end }}
</urlset>
